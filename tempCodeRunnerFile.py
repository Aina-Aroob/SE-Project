  # # Second line box (for result of our decision)
    # y += text_size_line1[1] + 20  # Add a little space between lines
    # cv2.rectangle(frame, 
    #             (x - 10, y - text_size_line2[1] - 10), 
    #             (x + text_size_line2[0] + 10, y + 10), 
    #             (0, 255, 0), -1)  # Green box for "Not Out" or "Out"
    # cv2.putText(frame, line2, (x, y), font, font_scale, (0, 0, 0), thickness, cv2.LINE_AA)
